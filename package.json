{
  "name": "q2nbarberia",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "homepage": "https://walterdif20.github.io/q2nbarberia",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/create-gh-pages-404.mjs",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "deploy:firebase": "npm run build && firebase deploy --only hosting",
    "deploy:firebase:rules": "firebase deploy --only firestore:rules,firestore:indexes",
    "deploy:firebase:functions": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase": "^12.15.0",
    "lucide-react": "^1.16.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "gh-pages": "^6.3.0",
    "vite": "^7.0.0"
  }
}
